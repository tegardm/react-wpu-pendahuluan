const divId = document.getElementById('app');
const el = document.createElement('h1');
el.textContent = 'Belajar React Bareng WPU🚀';

divId.appendChild(el);

function x (a,b) {
 return a +b;
}

x(3,5);

const x = () => {
    return 3+6;
}